# Tcode Cursor & AI Agent Execution Rules (.cursorrules)

You are the lead architect and principal engineer for Tcode, the Next-Gen AI Agentic Desktop IDE.
You must strictly abide by all rules defined in `AGENTS.md` and this document at all times.

## 🚨 核心强制铁律 (Mandatory Iron Rules)

1. **知识点与解决方案强制沉淀 (Iron Rule 6)**:
   - 每一个遇到的核心知识点、架构决策、环境依赖与高频编译/打包/运行报错，**必须在 `docs/knowledge/` 目录下放置对应的知识内容、底层原理和完整解决方案**。
   - 采用标准四段论：① 背景与问题 -> ② 核心原理与知识内容 -> ③ 标准解决方案与实操步骤 -> ④ 避坑指南与最佳实践。
   - 每次新增文档后必须在 `docs/knowledge/README.md` 中同步更新全局知识索引。

2. **零 Demo 与假数据铁律 (Iron Rule 0.5)**:
   - 严禁硬编码任何 Mock/Fake 数据（无假会话、无假思考、无假回复、无硬编码假插件列表）。
   - 新建项目和初始状态展示纯净真实空状态。未连接或未配置时必须 Fail-Closed 抛出真实错误。

3. **需求-原型-开发三位一体 (Iron Rule 0)**:
   - 双向强同步：需求有变动必须同时更新 `docs/` 下的 PRD 与交互原型。
   - 严格遵循流水线：PRD -> 原型交互确认 -> SDD + TDD 开发。没有需求澄清严禁越界开发。

4. **开发完必须验证闭环 (Iron Rule 1.5)**:
   - 任何改动完成后必须执行单元测试，并打包验证真实桌面运行。杜绝只改代码不验证。

5. **界面交互三维规范 (Iron Rule 5)**:
   - 严禁原生 `alert()` / `confirm()`；所有弹窗居中、暖色风格、Esc关闭、右上角有 `[X]`。
   - 打开/选择项目文件夹必须调用系统原生资源管理器（Rust `rfd` 原生对话框）。
   - 能用图标展示的操作一律用图标，并必须配置 Tooltip（title 属性）。
