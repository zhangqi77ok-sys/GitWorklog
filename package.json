{
  "name": "gitworklog-loop-control-plane",
  "private": true,
  "version": "0.1.0",
  "description": "Loop engineering control plane for AI coding runs",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build:packages && npm run build:apps",
    "build:apps": "npm run build --workspace @gitworklog/desktop && npm run build --workspace @gitworklog/web",
    "build:packages": "npm run build --workspace @gitworklog/shared-types && npm run build --workspace @gitworklog/db && npm run build --workspace @gitworklog/action-engine && npm run build --workspace @gitworklog/analyzers && npm run build --workspace @gitworklog/policy && npm run build --workspace @gitworklog/connectors && npm run build --workspace @gitworklog/evidence && npm run build --workspace @gitworklog/core",
    "dev": "npm run dev:desktop",
    "dev:web": "npm run dev --workspace @gitworklog/web",
    "dev:desktop": "npm run dev --workspace @gitworklog/desktop",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "typescript": "^5.6.3"
  }
}
