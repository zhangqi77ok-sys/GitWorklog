{
  "name": "@gitworklog/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "dev": "npm run build:packages --prefix ../.. && npm run build --workspace @gitworklog/web --prefix ../.. && npm run build && electron dist/main.js",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build:packages --prefix ../.. && npm run build && node --experimental-sqlite --test \"test/*.test.mjs\""
  },
  "dependencies": {
    "@gitworklog/connectors": "0.1.0",
    "@gitworklog/core": "0.1.0",
    "@gitworklog/db": "0.1.0",
    "@gitworklog/policy": "0.1.0",
    "@gitworklog/shared-types": "0.1.0"
  },
  "devDependencies": {
    "electron": "^33.4.11"
  }
}
