{
  "name": "@gitworklog/desktop",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "main": "src/main.ts",
  "scripts": {
    "dev": "echo \"desktop app shell placeholder\"",
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "npm run build:packages --prefix ../.. && npm run build && node --experimental-sqlite --test test/*.test.mjs"
  },
  "dependencies": {
    "@gitworklog/connectors": "0.1.0",
    "@gitworklog/core": "0.1.0",
    "@gitworklog/db": "0.1.0",
    "@gitworklog/policy": "0.1.0",
    "@gitworklog/shared-types": "0.1.0"
  }
}
