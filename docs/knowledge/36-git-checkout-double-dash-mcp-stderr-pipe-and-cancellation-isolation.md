# ???? 36: Git ??????????MCP Stderr ?????????????

---

## ? ???????? (Context & Problem Statement)

? Tcode Studio ????????????????? MCP ???????????????????????????????????????????????????

1. **Git ??????????? (git checkout -- <name>)**?
   - ?????????????????????? git checkout -- <name>?Git ???????-- ????????????????Pathspec?????????????????????????????????????Git ?? error: pathspec '<name>' did not match any file(s) known to git????????????
2. **MCP ???? Stderr ?????????????**?
   - StdioClient ???? MCP ??????? Node.js ? Python MCP Server???????? cmd.StdoutPipe??????? cmd.Stderr???????????? Debug ??????????????????Linux 64KB / Windows 4KB~64KB??????? write ?????????????? JSON-RPC ??????????????
3. **?????????? session_id ? IPC ?????**?
   - ?????????????????? CancelAgentStream ??? agent:interrupted ????? session_id??? wailsBridge.ts ?? handleFinish ?????? ID??? cleanAll() ???????Wails runtime ???????????????????? isStreaming ???????
4. **??? Git ?? (? HEAD ??) ? Diff ????**?
   - ?? git init ???????????????HEAD ??????Diff ??????? git diff HEAD -- <relPath>??? Git ?? fatal: bad revision 'HEAD'???? Diff ????????????

---

## ? ????????? (Knowledge Content & Root Cause)

### 1. Git Command Line Argument Disambiguation (--)
Git ?????????? checkout?diff?log?grep??????? revision?? commit?branch?tag?? path???????????????????????????????????
- git checkout <branch>???? HEAD ????????
- git checkout -- <path>?????????????
- -- ???? Git????????????????????????????? git checkout -- feature ??? Git ????? feature ??????????????

### 2. OS Pipe Buffer Limits and Deadlock in Go exec.Cmd
- ????????????????????????? Stderr ??????????????????????????????????????? IO ????????????
- ???????????? Stdout ???????????????? JSON-RPC ????????????????Deadlock??

### 3. IPC Event Lifecycle & Fail-Closed Cleanup
- ???????????????-???????????????????done?complete?interrupted?error?????????????????????????????????????????????

---

## ? ??????????? (Actionable Solutions & Step-by-Step Guide)

### 1. ?? Git ??????
? internal/gitops/gitops.go ??????? isValidBranchName ??????????? --?
`go
// CheckoutBranch ????????
func CheckoutBranch(workspace, name string) error {
	name = strings.TrimSpace(name)
	if !isValidBranchName(name) {
		return fmt.Errorf("invalid branch name: %q", name)
	}
	cmd := gitCmd(workspace, "checkout", name)
	out, err := cmd.CombinedOutput()
	if err != nil {
		return fmt.Errorf("git checkout failed: %s (%w)", strings.TrimSpace(string(out)), err)
	}
	return nil
}
`

### 2. MCP ??? Stderr ???????
? internal/mcp/stdio.go ? Start ????? cmd.StderrPipe()?????????????????
`go
	stderr, err := cmd.StderrPipe()
	if err != nil {
		c.mu.Unlock()
		return fmt.Errorf("stderr pipe error: %w", err)
	}
    // ...
	go func(r io.Reader) {
		buf := make([]byte, 4096)
		for {
			_, err := r.Read(buf)
			if err != nil {
				break
			}
		}
	}(stderr)
`

### 3. ?????? session_id ???????
?? CancelAgentStream ?????????? ID?
`go
	if a.ctx != nil {
		runtime.EventsEmit(a.ctx, "agent:interrupted", map[string]any{
			"session_id": a.currentSessionID,
			"message":    "???????????",
		})
	}
`
?? wailsBridge.ts ?????
`	s
      const handleFinish = (data: any) => {
        if (!data?.session_id || data.session_id === req.session_id) {
          cleanAll()
          if (callbacks.onDone) callbacks.onDone()
        }
      }
`

### 4. Git Diff ?? HEAD ????????
? internal/diff/differ.go ??? hasGitHead ???
`go
func hasGitHead(workspaceRoot string) bool {
	cmd := gitCmd(workspaceRoot, "rev-parse", "--verify", "HEAD")
	return cmd.Run() == nil
}
`
?????? HEAD??????????? git diff HEAD?

---

## ? ????????? (Troubleshooting & Best Practices)

1. **????????????**?
   - ?????? Git ????? --????????? -- ??????? git add -- <files> ? git checkout -- <paths> ?????????????????????
2. **????????????**?
   - ?????????????? CLI ???LSP ????MCP Server???????? Stdout???????? Stderr???? Stderr ??????????
3. **???????????**?
   - ?????????????????????? --silent-install-dir?????????????????????????????????
